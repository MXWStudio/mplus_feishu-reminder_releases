# 飞书表格中枢安装与使用

1. 从本仓库 Releases 下载 `MPlus-Feishu-Hub-Setup-0.11.1-x64.exe` 与 `SHA256SUMS.txt`，核对 SHA-256。
2. 在 Windows x64 中运行安装包，选择安装目录。安装结束不自动启动应用。
3. 如果该电脑正在运行源码版中枢，先退出原应用，再从桌面快捷方式启动 `MPlus Feishu Hub`。本次发布不自动迁移运行节点。
4. 打开 Chrome 扩展管理页 `chrome://extensions`，启用开发者模式，加载安装目录下的 `resources/collector-extension`。保留安装目录，不移动扩展文件夹。
5. 在控制台“连接与配置”显示配对码，在扩展中选择对应环境、保存配对码并连接。切换环境后刷新 M+ 页面。
6. 在 Chrome 登录 M+ 并打开素材审核管理页面，确认控制台显示采集连接。
7. 在控制台填写本机飞书连接配置。App Secret 通过当前 Windows 账户的系统加密存储保存；不从安装包导入任何账号或凭据。

应用仅向 M+ 发起白名单只读采集。飞书采集范围、群路由、统计起点和正式看板需要按本机的现有配置与验收规则管理。安装或下载本身不代表正式数据同步、群推送或全量业务对账已经通过。

Windows 持久配置会在应用运行时注册当前账号登录启动项；隔离验收配置不会覆盖它。卸载保留运行数据。已有正式节点升级时应保留既有恢复材料，并在退出原应用后操作。

本版本未进行代码签名，且不接入旧提醒版自动更新。安装包不包含本项目原始工程、测试、开发记录、数据库或 source map；Electron 与 Chrome 必需的压缩 JavaScript 运行代码仍可被分析，不属于源码加密产品。

0.11.1 修复了 0.11.0 安装后双击即退出的问题。若仍使用原始 0.11.0 安装包，请改用 0.11.1；升级前先退出应用。0.11.1 发布构建包含实际安装、最终程序启动及工作台交互检查。
